export default class {
    app_mode = "development"

    resource = [
        // Ex: { type: "css or js", src: "/resource/assets/css/style.css" },
    ]

    headers = {
        // "content-type": "application/json"
    }
}